# CONTEXT

포켓몬 TCG 카드 **도감** + **바인더 제작** 서비스.
실물 바인더를 채우기 전에 어떤 배치가 예쁜지 미리 보는 것이 이 앱의 존재 이유다.

- 스택: Next.js 15 (App Router) / React 19 / PostgreSQL 17 (Docker)
- 데이터 출처: [TCGdex](https://api.tcgdex.net/v2) — 카드 이미지는 CDN 핫링크, 조립하지 말고 `image_base` 원본을 저장해 쓴다
- 인증 없음. 모든 API가 `DEMO_USER_ID = 1` 고정 (`TODO: auth`)

## 용어

이 저장소의 코드·이슈·커밋은 아래 용어를 쓴다. 동의어로 흘러가지 말 것.

| 용어 | 뜻 |
|---|---|
| **도감** | 카드를 찾는 축. `/dex`. 탐색 전용이며 바인더와는 즐겨찾기로만 이어진다 |
| **바인더 제작** | 바인더를 만들고 카드를 배치하는 축. `/binders` |
| **대분류** | 도감의 1차 분류 7개. 즐겨찾기 · 카드 세트 · 포켓몬 도감번호 · 아티스트 · 트레이너 · 에너지 · etc |
| **묶음 축** | 대분류 중 세트·도감번호·아티스트. 전체 카드를 다른 기준으로 묶는다 |
| **즐겨찾기(찜)** | 카드에 대한 Y/N 토글. 수량 개념 없음. 도감↔바인더의 **유일한 접점** |
| **칸(slot)** | 바인더 한 장을 꽂는 자리. `binder_cards.position` 이 곧 칸 번호(0-based) |
| **면(page)** | 바인더 한 쪽. 칸 `rows × cols` 개. 1~60장 |
| **펼침면** | 마주 보는 두 면. 실물처럼 **1페이지는 오른쪽 단면**으로 시작하고 이후 (2,3) (4,5) 로 묶인다 |
| **커버** | 바인더 목록에 뜨는 대표 이미지. 세트 로고 중에서 고른다 |
| **클래식** | `trainer_type` 이 없는 트레이너. 2003년 이전엔 서포트·아이템 구분 자체가 없었다 — 결손이 아니다 |
| **etc** | 포켓몬인데 도감번호가 없는 카드. 라벨링 후 사라질 **임시** 분류 |
| ~~보관함~~ | **폐기됨.** 즐겨찾기로 대체. 되살리지 말 것 → [ADR-0001](docs/adr/0001-dogam-binder-separation.md) |

## 구조

```
/                  홈 — 인기 카드 캐러셀 + 도감/바인더 2버튼
/dex               도감  ?lang= &cat= &set=|dex=|artist=|type= &q= &p=
/binders           바인더 목록 (생성도 여기서)
/binders/[id]      개별 바인더 — 읽기 모드가 기본, 사이드바를 열면 편집 모드
```

도감의 탐색 상태는 **전부 URL 쿼리에 실린다.** 새로고침·뒤로가기·링크 공유가 그대로 동작한다.
대분류 목록·순서·언어 정의의 단일 출처는 [`lib/dex.ts`](lib/dex.ts)다.

**API**

| 경로 | 용도 |
|---|---|
| `/api/favorites` | GET 목록 / PUT 찜 / DELETE 해제 (둘 다 멱등) |
| `/api/cards/search` | 바인더 사이드바 검색. 조건이 하나도 없으면 결과를 쏟지 않는다 |
| `/api/binders` | 목록 / 생성 |
| `/api/binders/[id]` | 이름·크기·커버 변경 / 삭제 |
| `/api/binders/[id]/slots` | `place`(cardId) / `remove` / `move`(차 있으면 교환) |
| `/api/binders/[id]/pages` | 페이지 삽입·삭제. 뒤쪽 칸 번호를 통째로 민다 |

## 데이터 현황

2026-08-08 기준. TCGdex en + ja 전량 적재 완료.

| | 세트 | 카드 |
|---|---|---|
| en | 218 | 23,443 |
| ja | 177 | 8,159 |
| **합계** | **395** | **31,602** |

포켓몬 26,580 · 트레이너 4,366 · 에너지 656
포켓몬 종 1,026 · 아티스트 422 · 커버용 세트 로고 157(전부 EN) · etc 661

**이미지는 전량이 아니다** — ja 는 2020~2025-05 세트에만 있다(40%). TCGdex 원본에 `image` 필드가 없는 것이지 적재 누락이 아니다.
**이미지 유무로 설계를 바꾸지 않는다.** 한글·일본 데이터를 크롤링해 채울 예정이라, 지금 필터나 정렬을 걸면 나중에 걷어낼 부채가 된다.

## 스키마에서 주의할 점

- `binder_cards.position` = **칸 번호**(0-based). `(binder_id, position)` 유일. 페이지를 넣고 뺄 때 뒤쪽을 밀어야 하는데, 유일 제약 때문에 **음수 자리로 피신시켰다가 옮기는 2단계**로 처리한다
- `cards.dex_ids` 는 `NUMERIC[]`. 폼 변형 카드의 도감번호가 소수다(예: `384.1`)
- 도감번호를 읽을 땐 항상 `COALESCE(manual_dex_ids, dex_ids)`. 수작업 라벨이 TCGdex 원본보다 우선한다
- `card_group` 은 미사용. 언어 통합(en/ja 같은 카드 묶기)용으로 자리만 잡아둔 것
- **마이그레이션은 `db:migrate` 때마다 전부 재실행된다.** 반드시 멱등하게 쓰고, `DELETE`/`DROP` 로 사용자 데이터를 지우는 문장을 넣지 말 것

## 적재

```bash
npm run ingest -- --lang en --limit 0 --concurrency 10
npm run ingest -- --lang en --limit 0 --refetch Trainer,Energy   # 새 컬럼만 채울 때
```

이미 있는 카드는 건너뛴다 → 중간에 끊겨도 재실행하면 이어서 간다.
세트·카드 id 는 `encodeURIComponent` 로 감싼다 (`SM1+`, `exu-%3F` 같은 id 가 있다).

## 결정 기록

- [ADR-0001](docs/adr/0001-dogam-binder-separation.md) — 도감/바인더 분리, 대분류 7개, 보관함 폐기, 기각한 대안 6건

## 열려 있는 것

- **언어 이름 매핑 파일** — "피카츄"로 en/ja를 함께 검색. 이게 생기기 전까지 GLOBAL 은 단순 합집합이다
- **etc 661장 라벨링** — `manual_dex_ids` 컬럼과 우선 적용 로직은 준비돼 있다
- 한글·일본 데이터 크롤링 적재
- `/dex?cat=dex` 종 목록 1,025개에 페이지네이션 미적용 (약 900ms)
- 인증 (`DEMO_USER_ID` 제거)
