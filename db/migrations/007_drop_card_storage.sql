-- =====================================================================
-- card_storage 제거 (ADR-0001)
--
-- 보관함은 즐겨찾기(favorites)로 대체됐다. 006 에서는 이 테이블을 조회하는
-- 화면 코드가 남아 있어 데이터만 비우고 테이블은 남겨뒀는데,
-- 바인더 편집기를 사이드바 검색 방식으로 갈아엎으면서 마지막 참조가 사라졌다.
--
-- 이제 카드는 도감에서 '찜'하고, 바인더에서는 사이드바 검색으로 직접 찾아 배치한다.
-- =====================================================================

DROP TABLE IF EXISTS card_storage;
