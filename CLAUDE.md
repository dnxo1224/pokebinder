# pokebinder

포켓몬 TCG 카드 도감 + 개인 컬렉션 트래커 (Next.js 15 / React 19 / Postgres)

## Agent skills

### Issue tracker

이슈는 GitHub Issues(`dnxo1224/pokebinder`)에 있으며 `gh` CLI로 다룹니다. See `docs/agents/issue-tracker.md`.

### Triage labels

기본 5개 라벨(`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`)을 그대로 사용합니다. See `docs/agents/triage-labels.md`.

### Domain docs

single-context — 루트 `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
